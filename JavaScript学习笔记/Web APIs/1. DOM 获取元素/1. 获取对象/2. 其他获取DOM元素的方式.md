# 根据id获取一个元素
返回一个对象
```js
document.getElementById('nav')
```
# 根据 ==标签== 获取 ==一类== 元素
返回一个伪数组
```js
document.getElementsByTagName('div')
```
# 根据 类名 获取所有匹配的元素
返回一个伪数组
```js
document.getElementsByClassName('box')
```