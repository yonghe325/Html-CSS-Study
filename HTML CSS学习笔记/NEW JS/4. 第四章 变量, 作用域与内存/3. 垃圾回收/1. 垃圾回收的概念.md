JavaScript的垃圾回收, 类似于c++,c的内存管理(new, delete)
但JavaScript的垃圾回收是自动的, 会在每隔一定时间(或在预定时间)执行
%%自动垃圾回收是有标准%%
以实现 自动实现内存分配, 自动回收内存
==清理程序运行时, 会暂停脚本(Stop-the-World)运行==
因此有很多清理优化(GC优化)

但该机制不是完美的, 它不能准确区分那个变量还要使用, 那个变量已明确不再使用
所以该机制必须跟踪记录那个变量还会使用, 那个变量不再使用
所以需要标记策略对变量进行标记
在浏览器的发展史上，用到过两种主要的标记策略：
标记清理 和 引用计数