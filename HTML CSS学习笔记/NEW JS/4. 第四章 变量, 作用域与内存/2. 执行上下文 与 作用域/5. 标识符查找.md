当一个变量再被调用时, 会根据其上下文(其实是根据作用域链)进行查找
![[1. 作用域链#^1af7fd]]
当在本层上下文中找到同名变量(标识符), 就停止查找
否则到所有 包含(包含本层上下文的上下文, 就是所有祖先上下文)上下文 中进行查找, 且不能查找
使用块级作用域声明不会改变搜索的流程, 但会添加额外的搜索层级
>注意 标识符查找并非没有代价
>访问局部变量比访问全局变量要快, 因为不用切换作用域
>不过, JavaScript 引擎在优化标识符查找上做了很多工作, 将来这个差异可能就微不足道了