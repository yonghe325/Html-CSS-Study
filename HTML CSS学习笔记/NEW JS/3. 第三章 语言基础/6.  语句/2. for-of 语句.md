>for-of 语句是一种严格的迭代语句, 用于遍历可迭代对象的元素

```js
for (const el of [2,4,6,8]) {
	document.write(el);
}
```
+ for-of 循环会按照可迭代对象的 *next()方法*产生值的顺序迭代元素
	这与*可迭代对象*的特点有关
+ 如果for-of尝试迭代的对象不支持迭代, 则抛出异常
# for-await-of 语句
