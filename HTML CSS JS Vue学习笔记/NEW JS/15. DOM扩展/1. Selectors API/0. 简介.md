允许使用类似于CSS选择符的方式获取元素
而不是使用`getElementsID`或`getElementTagName`等选择器
Selectors API Level 1 的核心是两个方法：`querySelector()` 和 `querySelectorAll()`
Selectors API Level 2 规范在 Element 类型上新增了更多方法，比如 `matches()`, `find()`和`findAll()`
