渲染页面有两种模式 : 标准模式 , 混杂模式
	转到[[3.  文档模式|文档模式]]
`document.compatMode`属性有两个值 : 
1. `CSS1Compat` : 表示当前页面以标准模式渲染
2. `BackCompat` : 表示当前页面以混杂模式渲染
---
```js
if (document.compatMode == "CSS1Compat"){
	console.log("Standards mode");
} else {
	console.log("Quirks mode");
}
```