默认情况下
只要被观察的元素不被垃圾回收，MutationObserver 的回调就会响应 DOM 变化事件，从而被执行
要提前终止执行回调，可以调用 disconnect()方法
下面的例子演示了同步调用disconnect()之后，不仅会停止此后变化事件的回调
==也会抛弃已经加入任务队列要异步执行的回调==：
```js
let observer = new MutationObserver(() => console.log('<body> attributes changed'));
observer.observe(document.body, { attributes: true });
document.body.className = 'foo';
observer.disconnect();
document.body.className = 'bar';
//（没有日志输出）
```
要想让已经加入任务队列的回调执行
可以使用 setTimeout()让已经入列的回调执行完毕再调用 `disconnect()`：
```js
let observer = new MutationObserver(() => console.log('<body> attributes changed'));
observer.observe(document.body, { attributes: true });
document.body.className = 'foo';
setTimeout(() => {
	observer.disconnect();
	document.body.className = 'bar';
}, 0);
	// 在执行异步代码时在执行取消观察
	// 防止已经入队的回调删除
// <body> attributes changed
```

---
`disconnect()`方法仅会取消观察器的所有观察, 并不会终止该观察器的声明周期
	转到[[4. 复用MutationObserver|复用MutationObserver]]
	转到[[5. 重用MutationObserver|重用MutationObserver]]
