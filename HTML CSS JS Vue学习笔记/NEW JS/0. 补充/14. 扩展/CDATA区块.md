`CDATASection` 节点代表 XML 中的 **CDATA（Character Data，字符数据）节**
用于包裹**不需要被解析器解析的文本内容**
==CDATA 区块只在 XML 文档中有效==
在 HTML 中极少使用，主要用于 XML/XHTML 文档
```
<![CDATA[内容]]>
--------------------------------------------
<![CDATA[
  这里的内容不会被解析，可以包含任何字符
  包括: < & > " ' 等特殊符号
]]>
```