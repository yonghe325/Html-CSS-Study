ECMA6之前
JavaScript仅能通过将一个对象指向另一个对象的原型
来实现类与类之间的继承
```js Deepseek
// 核心思想：一个对象继承另一个对象
const animal = {
    eat() {
        console.log(`${this.name} is eating`);
    }
};
const dog = Object.create(animal);  // dog 的原型指向 animal
dog.name = 'Rex';
dog.eat();  // "Rex is eating"（继承自 animal）
```
