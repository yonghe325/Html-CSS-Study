Blob对象类似于一个容器, 可以存放任意类型的数据
包括图片, 视频, 文档, 甚至二进制数据
且一旦创建, 其内容就不可变, 保证了数据的完整性
在创建时, 为 Blob对象 对象指定一个标签, 用来表示数据的类型
# 主要功能
Blob对象有两个主要功能:
## 作为一个本地文件
## 生成临时的URL 
使用`URL.createObjectURL(blob)`方法
可以为这个blob对象在 内存中 生成唯一的 临时URL
这个URL可以用在任何需要URL的地方
常与`XMLSerializer`对象配合, 打开新文档