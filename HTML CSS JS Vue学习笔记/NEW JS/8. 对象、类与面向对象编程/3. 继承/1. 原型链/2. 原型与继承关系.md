确定原型与实例的关系两种方式
# 通过`instanceof`操作符
[[5. 确定类型 instanceof]]
如果一个实例中出现过相应的构造函数的原型
则`instanceof`返回`true`
# 通过`isPrototypeof()`方法
[[3. 关于原型的操作#`isPrototypeOf()`方法|isPrototypeof()方法]]
原型链中的每个原型都可以调用这个方法
只要原型链中包含这个原型，这个方法就返回 true
