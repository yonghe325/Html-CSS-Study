DOM1 主要定义了HTML 与 XML 的文档的底层结构
DOM2 与 DOM3 则在此基础上扩展了更多的文档交互功能, 提供了更高级的特性
实际上，DOM2 和 DOM3 是按照模块化的思路来制定标准的，每个模块之间有一定关联
这些模块如下
+ DOM Core：在 DOM1 核心部分的基础上，为节点增加方法和属性。
+ DOM Views：定义基于样式信息的不同视图。
+ DOM Events：定义通过事件实现 DOM 文档交互
	第十七章会专门介绍
+ DOM Style：定义以编程方式访问和修改 CSS 样式的接口。
+ DOM Traversal and Range：新增遍历 DOM 文档及选择文档内容的接口。
+ DOM HTML：在 DOM1 HTML 部分的基础上，增加属性、方法和新接口。
+ DOM Mutation Observers：定义基于 DOM 变化触发回调的接口
	这个模块是 DOM4 级模块，用于取代 Mutation Events。
	转到[[0. MutationObserver接口 监听器|MutationObserver接口]]

