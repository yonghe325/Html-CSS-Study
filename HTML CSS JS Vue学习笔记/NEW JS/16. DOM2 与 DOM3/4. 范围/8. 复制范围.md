# `cloneRange()`方法
调用范围的 `cloneRange()` 方法可以复制范围。这个方法会创建调用它的范围的副本
```js
let newRange = range.cloneRange();
```
新范围包含与原始范围一样的属性，修改其边界点不会影响原始范围

---
与 [[4. 操作范围#`cloneContents()`方法 -> 创建副本|cloneContents()]]方法类似
但是`cloneRange()`返回 `Range `对象
`cloneContents()`返回 `FragmentDocument`对象
