使用`alert`,`confirm`, `prompt`, 可以让浏览器调用系统对话框向用户显示消息
这些对话框都是同步的模态对话框
即在它们显示的时候，代码会停止执行
在它们消失以后，代码才会恢复执行
+ `alert()` : 它接收一个要显示给用户的字符串
	用户唯一的选择就是在看到警告框之后把它关闭
+ `confirm()`: 用户可以选择`Cancel`或`OK`
	同时根据用户的选择返回`true`/`fales`
+ `prompt()` : 从用户接受一串字符串
	如果用户选择`Cancel`, 则返回值为`Undefined`
	否则为用户输入的字符串