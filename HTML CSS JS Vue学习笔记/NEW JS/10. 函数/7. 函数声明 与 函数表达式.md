函数声明 和 函数表达式 在JavaScript 引擎在加载数据时对它们是区别对待的
JavaScript 引擎在任何代码执行之前，会先读取函数声明
并在执行上下文中生成函数定义
而函数表达式必须等到代码执行到它那一行，才会在执行上下文中生成函数定义
```js
// 没问题
console.log(sum(10, 10));
function sum(num1, num2) {
	return num1 + num2;
}
```
函数声明会在任何代码执行之前先被读取并添加到执行上下文
这个过程叫作 ==函数声明提升==（function declaration hoisting）
而使用表达式进行函数声明,只会使用将标识符进行提升
	转到[[1. var#提升(Hoisting)|提升(Hoisting)]]
```js
// 会出错
console.log(sum(10, 10));
let sum = function(num1, num2) {
	return num1 + num2;
};
```
上面的代码之所以会出错，是因为这个函数定义包含在一个变量初始化语句中，而不是函数声明中
这意味着代码如果没有执行到加粗的那一行
那么执行上下文中就没有函数的定义，所以上面的代码会出错
这并不是因为使用 let 而导致的，使用 var 关键字也会碰到同样的问题
==因为该提升只会提升声明, 而不会提升值==
	更多关于函数提升的问题
	转到[[11. 函数表达式#函数表达式 与 函数声明的提升问题|函数表达式 与 函数声明的提升问题]]
```js
//也会出错
console.log(sum(10, 10));
var sum = function(num1, num2) {
	return num1 + num2;
};
```
除了函数什么时候真正有定义这个区别之外，这两种语法是等价的
在使用函数表达式初始化变量时，也可以给函数一个名称
比如 `let sum =function sum() {}`
更多关于函数表达式, 转到[[11. 函数表达式|函数表达式]]
