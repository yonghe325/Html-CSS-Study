
Promise 的设计很大程度上会导致一种完全不同于 JavaScript 的计算模式
下面展示了两种模式下抛出错误的情形
```js
try {
	throw new Error('foo');
} catch(e) {
	console.log(e); // Error: foo
}


try {
	Promise.reject(new Error('bar'));
	//Promise.reject()是同步操作
	//但是在异步操作中抛出错误
} catch(e) {
	console.log(e);
}
// Uncaught (in promise) Error: bar
```
>第一个 try/catch 抛出并捕获了错误，第二个 try/catch 抛出错误却没有捕获到
>乍一看这可能有点违反直觉
>因为代码中确实是同步创建了一个拒绝的期约实例，而这个实例也抛出了包含拒绝理由的错误
>这里的同步代码之所以没有捕获期约抛出的错误
>是因为它==没有通过异步模式捕获错误==
>	因为这里抛出的是 ==异步错误==, 发生在同步代码执行之后
>从这里就可以看出期约真正的异步特性：
>	==它们是同步对象==(在同步执行模式中使), 但也是==异步执行模式的媒介==

>在前面的例子中，拒绝期约的错误并没有抛到执行同步代码的线程里
>而是通过浏览器异步消息队列来处理的
>因此，`try/catch` 块并不能捕获该错误
>代码一旦开始以异步模式执行
>则唯一与之交互的方式就是使用异步结构——更具体地说，就是==期约的方法==
>	转到[[2. then()方法|then()方法]]

