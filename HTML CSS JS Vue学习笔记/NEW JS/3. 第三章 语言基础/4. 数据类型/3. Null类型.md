`Null`类型同样只有一个值, 就是`Null`
当声明一个变量, 但不确定他要被赋值为何值时
就可以用`Null`对其进行初始化
而后就只需检查该变量是否为`Null`, 再决定是否为其赋值
```js
if (car != null) {
	// car 是一个对象的引用
}
```
`undefined`值 是由 `Null`值派生而来的
所以ECMA-262 中, 认为他们 表面值 相等
即
```js
console.log(null == undefined);
//true
```
`Null`值同样也是一个假值, 被认为是`false`