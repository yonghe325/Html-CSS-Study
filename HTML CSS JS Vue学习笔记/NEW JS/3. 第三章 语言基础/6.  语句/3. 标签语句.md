类似于c++中的标签
目的是更准确的跳出循环
配合`break` 与 `continue` 使用
```js
let num = 0;
outermost://标识第一个for循环
for (let i = 0; i < 10; i++) {
	for (let j = 0; j < 10; j++) {
		if (i == 5 && j == 5) {
		break outermost;//返回到标记处
		}
		num++;
	}
}
console.log(num); // 55
```
`break`会跳出标记的整个循环
`continue`会跳过标记的循环, 执行下一次迭代