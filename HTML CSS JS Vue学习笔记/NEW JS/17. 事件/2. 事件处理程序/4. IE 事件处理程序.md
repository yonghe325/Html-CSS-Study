与DOM2 赋值和移除事件处理程序两个方法对应
IE 事件处理程序的这两个方法分别是`attachEvent()` 与 `detachEvent()`
与DOM2的两个方法不同的是
+  事件名要在前面加上`on`, 例如
	DOM2 中为`click`, IE中为`onclick`
+ ==IE的事件处理函数中的`this`不是该元素本身, 而是`window`对象==

其他地方与DOM2的两个方法相同