IE 事件流被称为事件冒泡，这是因为事件被定义为从最具体的元素(文档树中最深的节点)开始触发
然后向上传播至没有那么具体的元素(文档)
	与事件捕获完全相反
比如有如下 HTML 页面
```js
<!DOCTYPE html>
<html>
	<head>
		<title>Event Bubbling Example</title>
	</head>
	<body>
		<div id="myDiv">Click Me</div>
	</body>
</html>
```
在点击页面中的\<div>元素后，click 事件会以如下顺序发生：
1. \<div>
2. \<body>
3. \<html>
4. document


也就是说，\<div>元素，即被点击的元素，最先触发 click 事件
然后，click 事件沿 DOM 树一路向上，在经过的每个节点上依次触发，直至到达 document 对象
![[Pasted image 20260613103318.png]]
所有现代浏览器都支持事件冒泡，只是在实现方式上会有一些变化
IE5.5及早期版本会跳过\<html>元素（从\<body>直接到 document）
现代浏览器中的事件会一直冒泡到 `window` 对象
