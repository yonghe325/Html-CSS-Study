与Date类型类似
RegExp类型也可以通过`RegExp()`构造函数进行实例
```js
let expression = new RegExp("pattern","flags")
```
`pattern`是正则表达式, `flags`是模式
这两个参数都是字符串
所以所有转义字符都要==二次转义==
![[Pasted image 20260208172614.png]]
## 二次修改
使用`RegExp()`函数也可以基于已有的正则表达式实例
并可选择性的修改他们的标记
```js
const re1 = /cat/g;
console.log(re1); // "/cat/g"

const re2 = new RegExp(re1);
console.log(re2); // "/cat/g"

const re3 = new RegExp(re1, "i");
console.log(re3); // "/cat/i"
```