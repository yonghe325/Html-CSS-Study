外部js文件有以下优点 : 
+ 可维护性
+ 缓存 : 
	+ 当多个页面都用到同一个文件, 则该文件只需下载一次, 页面加载更快
+ 适应未来
外部js文件有以下注意的特点 : 
+ 带宽 : 每次 *预请求* 外部文件, 都会占用带宽
	在*SPDY/HTTP2*中 , 以轻量 独立 JavaScript 组件形式向客户端送达脚本更具优势
	简单来说就是从同一个地方加载多个文件时, 会将这些文件放入缓存中
	等需要用时, 在从缓存中拿取