# parentNode
获取该对象的父节点
找不到返回NULL
# childrenNodes
返回所有的子节点
==包括元素节点,文本节点, 属性节点==, ...
# children
只返回==所有的子元素节点==
返回一个伪数组
找不到返回NULL
# nextElementSibling
==上一个==兄弟元素节点
# previousElementSibling
==下一个==兄弟元素节点